# Phase 2 rendezvous setup for the XPMultiCrew plugin (docs/plan.md
# section 7). One directive per line, # comments and blank lines ignored.
#
# Copy this file to "XPMultiCrew_rendezvous.txt" next to where you start
# X-Plane from (its working directory), or point the plugin at it via the
# XPMULTICREW_RENDEZVOUS_FILE environment variable.
#
# Whoever starts first:
#
#   SERVER your-vps.example.com:45000
#   CREATE
#
# Everyone else, once they have the code the first person's Log.txt
# printed ("XPMultiCrew: rendezvous session ready - code '...'"):
#
#   SERVER your-vps.example.com:45000
#   JOIN 2K2SYJ
#
# Treat the session code like a shared secret for now (see server/README.md
# "not yet done" - no auth/encryption yet, so anyone who has the code can
# join).
